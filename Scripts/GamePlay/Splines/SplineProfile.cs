using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SplineProfile : MonoBehaviour
{
    public string trackType;
    // Update is called once per frame
    void Update()
    {
        if (trackType == "line")
        {
            
        }
        
    }
}
